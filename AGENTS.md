# POW! Peptides working instructions

- Run all development, installation, tests, and builds on the Mac mini. The MacBook Pro only remotely operates the Mini. Process Supreme projects belong under `/Users/johnny/Projects`, each connected to its corresponding GitHub repository.
- This project's canonical checkout is `/Users/johnny/Projects/pow-peptides`; `origin` must be `https://github.com/processsupreme/pow-peptides-prototype.git`. Verify hardware, directory, remote, branch, and working tree before changes. Stop on an environment mismatch. Do not publish hardware identifiers.
- Read `README.md`, `docs/DEVELOPMENT.md`, `docs/PROJECT_STATUS.md`, and applicable nested instructions. Preserve existing uncommitted work. Create a uniquely named feature branch with `git switch -c`; never overwrite a branch or work directly on `main`.
- Current phase: interactive product specification. Prioritize complete, coherent demo journeys with synthetic data and explicit simulated states. Production identity, commerce, recurring billing, affiliate services, and delivery configuration are deferred until closer to launch; their absence must not block prototype development. Document unresolved requirements without inventing business rules or requiring provider selection now.
- Preserve the implementation and approved assets in `public/brand-assets`; follow `POW_BRAND_ASSET_GUIDE.md`. Keep the bold superhero POW! direction, electric-lime exclamation point, and black/white/lime identity. Do not redesign without a request.
- Use the existing Node requirement and npm lockfile. Install missing dependencies with `npm ci`; avoid unrelated upgrades and automatic audit fixes. Run `npm run lint`, `npm test`, and `npm run vercel-build` locally for relevant implementation changes. Document warnings and coverage limits.
- Preserve both Vercel and Sites configuration and hosting identity, including `.openai/hosting.json`. A local build is not a deployment. Do not trigger remote builds. Before any push or PR, verify external build triggers; if Mini-only execution cannot be confirmed, retain a local commit and report the unresolved configuration. Merge and deployment require a separate user instruction.
- This is a public prototype repository. Never commit credentials, internal client costs, customer information, local environment files, or generated build output. Use synthetic data. Treat displayed demo prices, discounts, terms, test results, and operational rules as unapproved unless explicitly supplied and approved.
- Scope changes to the user's task; do not silently connect production integrations. Stage explicit task files, review the staged diff, and commit only those changes. Report checks, commit, push status, and remaining gaps.
